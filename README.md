Sources for the plugin `plugin.playfab.client`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.playfab.client" = {
            publisherId = "com.playfab",
        },
    },
}
```
